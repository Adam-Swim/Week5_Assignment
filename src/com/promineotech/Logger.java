package com.promineotech;

public interface Logger {

	public void log(String message);
	public void error(String message);
	
		// created an interface called Logger with two methods, Log and Error.
}
